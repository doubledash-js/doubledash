import settings from './settings';
import off from './off';
import on from './on';
import once from './once';
import trigger from './trigger';

export default {
    settings,
    off,
    on,
    once,
    trigger
}
